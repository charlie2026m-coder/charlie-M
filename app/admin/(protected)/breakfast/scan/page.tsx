import { DoorScanner } from '@/app/_components/breakfast/DoorScanner'

// The panel's menu is the way back; the scanner needs no button of its own here.
export default function BreakfastScanPage() {
  return <DoorScanner />
}
